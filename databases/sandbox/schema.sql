


CREATE TABLE bank(age BIGINT, job VARCHAR, marital VARCHAR, education VARCHAR, "default" VARCHAR, balance BIGINT, housing VARCHAR, loan VARCHAR, contact VARCHAR, "day" BIGINT, "month" VARCHAR, duration BIGINT, campaign BIGINT, pdays BIGINT, previous BIGINT, poutcome VARCHAR, y VARCHAR);
CREATE TABLE income(age BIGINT, sex VARCHAR, education VARCHAR, "education-num" BIGINT, "marital-status" VARCHAR, workclass VARCHAR, occupation VARCHAR, "hours-per-week" BIGINT, income VARCHAR, "label" BIGINT);
CREATE TABLE iris("sepal length (cm)" DOUBLE, "sepal width (cm)" DOUBLE, "petal length (cm)" DOUBLE, "petal width (cm)" DOUBLE, target DOUBLE);
CREATE TABLE NYCTaxiFares(pickup_datetime TIMESTAMP, fare_amount DOUBLE, fare_class BIGINT, pickup_longitude DOUBLE, pickup_latitude DOUBLE, dropoff_longitude DOUBLE, dropoff_latitude DOUBLE, passenger_count BIGINT);
CREATE TABLE recipes("name" VARCHAR, instructions VARCHAR, cook_time BIGINT, creator VARCHAR);




